const form = document.getElementById('form');

form.addEventListener('submit', (e) => {
    e.preventDefault();

    //Get book info
    const author = document.getElementById('author_input');
    const title = document.getElementById('book_title');
    const pages = document.getElementById('total_pages');
    const read = document.getElementById('readtf');
    
})